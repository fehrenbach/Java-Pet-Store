JSF JavaScript Wrapper Component
--------------------------------

For now we are only including the Dojo examples. Later we may show more. 
To build and deploy do the following:

Type:

ant

ant deploy-all


----------

The Dojo components are defined in the "dojo" directory.

The wrapper component itself is declared in the "shared" directory.

